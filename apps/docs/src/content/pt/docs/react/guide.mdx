---
title: Guia para React
description: Monte sliders com @reelkit/react. Componente Reel, padrão itemBuilder, navegação, ReelIndicator, virtualização com mais de 10000 itens e dicas de desempenho.
---

import { Hand, Keyboard, MousePointer, InfinityIcon, Radio, Navigation, Zap, Layers, Code } from 'lucide-react';
import { BasicSliderDemo } from '../../../../components/demos/BasicSliderDemo';
import { InfiniteListDemo } from '../../../../components/demos/InfiniteListDemo';
import { GrowableListDemo } from '../../../../components/demos/GrowableListDemo';
import reelComponent from '../../../snippets/react-guide/reel-component.tsx?raw';
import autoSizing from '../../../snippets/react-guide/auto-sizing.tsx?raw';
import itembuilderPattern from '../../../snippets/react-guide/itembuilder-pattern.tsx?raw';
import navigation from '../../../snippets/react-guide/navigation.tsx?raw';
import urlState from '../../../snippets/react-guide/url-state.tsx?raw';
import reelindicator from '../../../snippets/react-guide/reelindicator.tsx?raw';
import basicFullCode from '../../../snippets/react-guide/basic-full-code.tsx?raw';
import infiniteFullCode from '../../../snippets/react-guide/infinite-full-code.tsx?raw';
import growableFullCode from '../../../snippets/react-guide/growable-full-code.tsx?raw';

Aprenda a montar sliders com `@reelkit/react`.

<FeatureGrid columns={3} items={[
              {
                icon: Hand,
                label: 'Toque em primeiro lugar',
                desc: 'Deslize com inércia e encaixe',
              },
              {
                icon: Keyboard,
                label: 'Navegação por teclado',
                desc: 'Setas e Escape',
              },
              {
                icon: MousePointer,
                label: 'Roda do mouse',
                desc: 'Opcional, com debounce',
              },
              {
                icon: InfinityIcon,
                label: 'Virtualizado',
                desc: 'Mais de 10.000 itens, 3 no DOM',
              },
              {
                icon: Radio,
                label: 'Indicadores',
                desc: 'Pontinhos deslizantes no estilo Instagram',
              },
              {
                icon: Navigation,
                label: 'API programática',
                desc: 'next(), prev(), goTo() pelo ref',
              },
              {
                icon: Zap,
                label: 'Modo cíclico',
                desc: 'Navegação circular sem fim',
              },
              {
                icon: Layers,
                label: 'Nos dois sentidos',
                desc: 'Vertical ou horizontal',
              },
              {
                icon: Code,
                label: 'Zero re-renderizações',
                desc: 'Estado atualizado por sinais',
              },
            ]} />

## Componente Reel [#reel-component]

O componente `Reel` é o contêiner principal: cuida do estado do slider, dos gestos de toque, da navegação por teclado e das animações.

<CodeBlock code={reelComponent} language="tsx" />

## Dimensionamento automático [#auto-sizing]

A prop `size` é opcional. Sem ela, o Reel mede o próprio contêiner com `ResizeObserver` e acompanha o layout definido no CSS. O contêiner precisa ganhar tamanho do elemento pai (flex, grid ou dimensões explícitas no CSS).

<CodeBlock code={autoSizing} language="tsx" />

## Padrão itemBuilder [#itembuilder-pattern]

A prop `itemBuilder` é uma função que recebe o índice e devolve o conteúdo de cada slide. Esse padrão é o que viabiliza a virtualização — apenas os itens visíveis são renderizados.

<CodeBlock code={itembuilderPattern} language="tsx" />

## Navegação [#navigation]

Formas de navegação já incluídas:

<Bullets marker="dot">

- **Toque/Deslizar:** arraste para navegar, com inércia e encaixe
- **Teclado:** setas e Escape
- **Roda do mouse:** ative pela prop `enableWheel`
- **Programática:** use `apiRef` para `next()`, `prev()`, `goTo()`

</Bullets>

<CodeBlock code={navigation} language="tsx" />

## Estado na URL [#url-state]

`useOverlayUrlState` monta um controlador de estado na URL para o overlay e devolve o controlador inteiro; você o entrega a um `*UrlOverlay` pela prop `controller`. Quem manda no estado aberto é a URL, então um overlay ligado a ela se abre sozinho e o link vira a forma normal de abrir. A primeira escrita de um parâmetro ausente empilha uma entrada no histórico e cada escrita seguinte a substitui, então percorrer os itens nunca soterra o botão voltar. Guarde o controlador para ler `value`/`position` e para conduzi-lo por código: `set(position)` abre, `set(null)` fecha, e `set` é exatamente a mesma escrita de baixo nível que o overlay usa internamente ao trocar de slide.

<CodeBlock code={urlState} language="tsx" />

O objeto de opções recebe `param`, `codec` e `locator` (os três obrigatórios), mais um `adapter` opcional. `param` e `adapter` são lidos na primeira renderização e ficam fixos depois disso — remonte o componente para trocá-los —, enquanto `codec` e `locator` são lidos ao vivo, então uma busca feita depois de a lista crescer ou mudar de ordem enxerga a lista atual. O `codec` e o `locator` formam um par combinado que divide o mesmo `Id`, por isso andam juntos — em uma galeria simples com `?photo=3`, espalhe `...urlIndexKey(() => images.length)`, que devolve as duas metades de uma vez. `urlIndexKey` liga o parâmetro a um índice de slide e o limita pela contagem atual que o getter devolve, então um `?photo=99` velho ou fora do intervalo é rejeitado e se cura sozinho saindo da URL, em vez de abrir um slide que ninguém nomeou. Passe um getter, e não um número, para que o limite continue certo à medida que um feed paginado cresce. Por baixo, ele embrulha `createIndexLocator` (a metade do locator) e a combina com `indexCodec`. Um feed paginado ou uma galeria endereçada por identidade fornece o próprio par `codec` + `locator`. A tabela completa de opções está na [referência da API para React](/docs/react/api#useoverlayurlstate).

## ReelIndicator [#reelindicator]

Componente opcional que mostra indicadores de progresso no estilo Instagram, sinalizando a posição atual dentro do slider. Colocado dentro de um `Reel`, ele se liga sozinho aos valores de `count` e `active` do componente pai pelo contexto — sem estado ligado à mão.

<CodeBlock code={reelindicator} language="tsx" />

## Demonstração ao vivo: slider básico [#live-demo-basic-slider]

<FeatureGrid items={[
              { icon: Hand, label: 'Toque/Deslize', desc: 'Com inércia' },
              {
                icon: Keyboard,
                label: 'Teclado',
                desc: 'Setas e Escape',
              },
              { icon: Layers, label: 'Indicadores', desc: 'No estilo Instagram' },
              { icon: Navigation, label: 'Navegação', desc: 'Pelo apiRef' },
            ]} />

<Sandbox
  code={basicFullCode}
  title="BasicSlider.tsx"
  height={500}
  stackblitzDeps={['@reelkit/react']}
  stackblitzExtraDeps={{ 'lucide-react': '^0.562.0' }}
>
  <BasicSliderDemo />
</Sandbox>

<Caption>Experimente — clique nos botões para percorrer os slides.</Caption>

## Pontos principais [#key-points]

<KeyPoints>
<KeyPoint title="prop size">
  Tupla \[largura, altura\] opcional; sem ela, o tamanho vem do CSS
</KeyPoint>
<KeyPoint title="itemBuilder">
  Recebe o índice e devolve o conteúdo do slide
</KeyPoint>
<KeyPoint title="apiRef">
  Dá acesso aos métodos do controlador para navegar
</KeyPoint>
<KeyPoint title="afterChange">
  Acompanha o índice atual para atualizar a interface
</KeyPoint>
</KeyPoints>

## Demonstração ao vivo: lista infinita [#live-demo-infinite-list]

O reelkit mantém apenas **3 slides no DOM** a qualquer momento (atual, anterior, próximo). É isso que deixa a rolagem fluida em listas com mais de 10.000 itens.

<FeatureGrid items={[
              {
                icon: Zap,
                label: '3 itens no DOM',
                desc: 'Só os slides visíveis são renderizados',
              },
              {
                icon: Zap,
                label: 'Mais de 10.000 itens',
                desc: 'Sem engasgos em nenhuma escala',
              },
              {
                icon: Zap,
                label: 'Memória constante',
                desc: 'Os mesmos 3 nós no DOM, seja qual for a contagem',
              },
              {
                icon: Zap,
                label: 'goTo(n)',
                desc: 'Salte para qualquer índice na hora',
              },
            ]} />

<Sandbox
  code={infiniteFullCode}
  title="InfiniteList.tsx"
  height={500}
  stackblitzDeps={['@reelkit/react']}
  stackblitzExtraDeps={{ 'lucide-react': '^0.562.0' }}
>
  <InfiniteListDemo />
</Sandbox>

<Caption>10.000 itens — apenas 3 no DOM. Use os botões ou digite um número para saltar.</Caption>

## Demonstração ao vivo: lista que cresce [#live-demo-growable-list]

Simula um feed infinito em que os itens chegam conforme a necessidade — como no TikTok ou no Instagram. Comece com 20 itens, role até perto do fim e veja novos lotes chegarem sozinhos.

<FeatureGrid items={[
              {
                icon: Zap,
                label: 'Contagem dinâmica',
                desc: 'Os itens carregam conforme você rola',
              },
              {
                icon: Zap,
                label: 'Carga em lotes',
                desc: '20 itens por lote',
              },
              {
                icon: Layers,
                label: 'Virtualizado',
                desc: 'Ainda apenas 3 no DOM',
              },
              {
                icon: Radio,
                label: 'Indicador automático',
                desc: 'Os pontinhos crescem junto com o conteúdo',
              },
            ]} />

<Sandbox
  code={growableFullCode}
  title="GrowableList.tsx"
  height={500}
  stackblitzDeps={['@reelkit/react']}
  stackblitzExtraDeps={{ 'lucide-react': '^0.562.0' }}
>
  <GrowableListDemo />
</Sandbox>

<Caption>Role até o fim — novos itens chegam sozinhos. O contador e o indicador crescem a cada lote.</Caption>

## Dicas de desempenho [#performance-tips]

<KeyPoints>
<KeyPoint title="Memorize os arrays de dados">
  Envolva seu array de itens com `useMemo`. Uma nova referência de array a cada renderização dispara uma atualização de `count` e o recálculo dos intervalos visíveis.
</KeyPoint>
<KeyPoint title="Deixe o itemBuilder leve">
  Ele roda a cada mudança do intervalo visível (normalmente 3 slides). Evite cálculos pesados ou efeitos colaterais lá dentro.
</KeyPoint>
<KeyPoint title="Carregue os dados perto da borda">
  Use `afterChange` para perceber quando o leitor se aproxima do fim e busque o lote seguinte antes que os slides acabem (veja a demonstração da lista que cresce, acima).
</KeyPoint>
<KeyPoint title="Desligue a roda em páginas roláveis">
  Defina `enableWheel={false}` quando o slider estiver dentro de um layout rolável, para não capturar a rolagem da página.
</KeyPoint>
</KeyPoints>

<NextSteps
        items={[
          {
            label: 'Referência da API',
            path: '/docs/react/api',
            description: 'todas as props disponíveis',
          },
          {
            label: 'Reel Player',
            path: '/docs/reel-player',
            description: 'player de vídeo no estilo TikTok/Reels',
          },
          {
            label: 'Lightbox',
            path: '/docs/lightbox',
            description: 'galeria de imagens e vídeos',
          },
          {
            label: 'Stories Player',
            path: '/docs/stories-player',
            description: 'visualizador de stories no estilo Instagram',
          },
        ]}
      />
