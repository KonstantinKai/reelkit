---
title: Guía de React
description: Crea sliders con @reelkit/react. El componente Reel, el patrón itemBuilder, la navegación, ReelIndicator, la virtualización con más de 10.000 elementos y consejos de rendimiento.
---

import { Hand, Keyboard, MousePointer, InfinityIcon, Radio, Navigation, Zap, Layers, Code } from 'lucide-react';
import { BasicSliderDemo } from '../../../../components/demos/BasicSliderDemo';
import { InfiniteListDemo } from '../../../../components/demos/InfiniteListDemo';
import { GrowableListDemo } from '../../../../components/demos/GrowableListDemo';
import reelComponent from '../../../snippets/react-guide/reel-component.tsx?raw';
import autoSizing from '../../../snippets/react-guide/auto-sizing.tsx?raw';
import itembuilderPattern from '../../../snippets/react-guide/itembuilder-pattern.tsx?raw';
import navigation from '../../../snippets/react-guide/navigation.tsx?raw';
import urlState from '../../../snippets/react-guide/url-state.tsx?raw';
import reelindicator from '../../../snippets/react-guide/reelindicator.tsx?raw';
import basicFullCode from '../../../snippets/react-guide/basic-full-code.tsx?raw';
import infiniteFullCode from '../../../snippets/react-guide/infinite-full-code.tsx?raw';
import growableFullCode from '../../../snippets/react-guide/growable-full-code.tsx?raw';

Aprende a crear sliders con `@reelkit/react`.

<FeatureGrid columns={3} items={[
              {
                icon: Hand,
                label: 'Táctil ante todo',
                desc: 'Deslizamiento con inercia y ajuste',
              },
              {
                icon: Keyboard,
                label: 'Teclado',
                desc: 'Flechas + Escape',
              },
              {
                icon: MousePointer,
                label: 'Rueda del ratón',
                desc: 'Opcional, con debounce',
              },
              {
                icon: InfinityIcon,
                label: 'Virtualizado',
                desc: 'Más de 10.000 elementos, 3 en el DOM',
              },
              {
                icon: Radio,
                label: 'Indicadores',
                desc: 'Puntos que se desplazan al estilo de Instagram',
              },
              {
                icon: Navigation,
                label: 'API por código',
                desc: 'next(), prev(), goTo() mediante ref',
              },
              {
                icon: Zap,
                label: 'Modo bucle',
                desc: 'Navegación circular infinita',
              },
              {
                icon: Layers,
                label: 'Dirección',
                desc: 'Vertical u horizontal',
              },
              {
                icon: Code,
                label: 'Sin re-renderizados',
                desc: 'Actualizaciones de estado con señales',
              },
            ]} />

## Componente Reel [#reel-component]

El componente `Reel` es el contenedor principal: gestiona el estado del slider, los gestos táctiles, la navegación con teclado y las animaciones.

<CodeBlock code={reelComponent} language="tsx" />

## Tamaño automático [#auto-sizing]

La prop `size` es opcional. Sin ella, Reel mide su contenedor con `ResizeObserver` y se adapta al diseño que marque el CSS. El tamaño del contenedor lo tiene que dar su padre (por ejemplo, flex, grid o dimensiones explícitas en CSS).

<CodeBlock code={autoSizing} language="tsx" />

## Patrón itemBuilder [#itembuilder-pattern]

La prop `itemBuilder` es una función que recibe el índice y devuelve el contenido de cada slide. Este patrón hace posible la virtualización: solo se renderizan los elementos visibles.

<CodeBlock code={itembuilderPattern} language="tsx" />

## Navegación [#navigation]

Métodos de navegación incluidos:

<Bullets marker="dot">

- **Táctil / deslizar:** arrastra para navegar con inercia y ajuste al slide
- **Teclado:** teclas de flecha y Escape
- **Rueda del ratón:** se activa con la prop `enableWheel`
- **Por código:** usa `apiRef` para `next()`, `prev()`, `goTo()`

</Bullets>

<CodeBlock code={navigation} language="tsx" />

## Estado en la URL [#url-state]

`useOverlayUrlState` crea un controlador de estado en la URL para un overlay y lo devuelve entero; después se lo pasas a un `*UrlOverlay` en su prop `controller`. La URL es dueña del estado abierto, así que un overlay vinculado se abre solo y lo habitual es abrirlo con un enlace. La primera escritura de un parámetro ausente añade una entrada al historial y las siguientes la sustituyen, así que pasar slides nunca entierra el botón de volver. Guarda el controlador para leer `value`/`position` y manejarlo por código: `set(position)` abre, `set(null)` cierra, y `set` es la misma escritura de bajo nivel que usa el overlay internamente al cambiar de slide.

<CodeBlock code={urlState} language="tsx" />

El objeto de opciones recibe `param`, `codec` y `locator` (los tres obligatorios), más un `adapter` opcional. `param` y `adapter` se leen en el primer renderizado y quedan fijos (vuelve a montar para cambiarlos), mientras que `codec` y `locator` se leen en cada uso, así que una búsqueda después de que la lista crezca o se reordene ve la lista actual. El `codec` y el `locator` son un par a juego que comparte el mismo `Id`, así que van juntos: para una galería simple con `?photo=3`, usa `...urlIndexKey(() => images.length)`, que devuelve las dos mitades a la vez. `urlIndexKey` convierte el parámetro en el índice de un slide y lo limita con el recuento actual que devuelve el getter, así que un `?photo=99` desfasado o fuera de rango se rechaza y se limpia solo de la URL en lugar de abrir un slide que nunca se nombró. Pasa un getter y no un número para que el límite siga siendo correcto mientras crece un feed paginado. Envuelve `createIndexLocator` (la mitad del locator) y lo combina con `indexCodec`. Un feed paginado o una galería por identidad aporta su propio par de `codec` + `locator`. La tabla completa de opciones está en la [referencia de la API de React](/docs/react/api#useoverlayurlstate).

## ReelIndicator [#reelindicator]

Componente opcional que muestra indicadores de progreso al estilo de Instagram con la posición actual en el slider. Colocado dentro de un `Reel`, se conecta solo a los valores `count` y `active` del padre a través del contexto, sin conectar el estado a mano.

<CodeBlock code={reelindicator} language="tsx" />

## Demo en vivo: slider básico [#live-demo-basic-slider]

<FeatureGrid items={[
              { icon: Hand, label: 'Táctil / deslizar', desc: 'Con inercia' },
              {
                icon: Keyboard,
                label: 'Teclado',
                desc: 'Flechas + Escape',
              },
              { icon: Layers, label: 'Indicadores', desc: 'Al estilo de Instagram' },
              { icon: Navigation, label: 'Navegación', desc: 'Mediante apiRef' },
            ]} />

<Sandbox
  code={basicFullCode}
  title="BasicSlider.tsx"
  height={500}
  stackblitzDeps={['@reelkit/react']}
  stackblitzExtraDeps={{ 'lucide-react': '^0.562.0' }}
>
  <BasicSliderDemo />
</Sandbox>

<Caption>Pruébalo: pulsa los botones para moverte entre los slides.</Caption>

## Puntos clave [#key-points]

<KeyPoints>
<KeyPoint title="Prop size">
  Tupla opcional \[width, height\], u omítela para que el CSS marque el tamaño
</KeyPoint>
<KeyPoint title="itemBuilder">
  Recibe el índice y devuelve el contenido del slide
</KeyPoint>
<KeyPoint title="apiRef">
  Da acceso a los métodos del controlador para navegar
</KeyPoint>
<KeyPoint title="afterChange">
  Sigue el índice actual para actualizar la interfaz
</KeyPoint>
</KeyPoints>

## Demo en vivo: lista infinita [#live-demo-infinite-list]

reelkit renderiza solo **3 slides en el DOM** en todo momento (el actual, el anterior y el siguiente). Así el desplazamiento es fluido en listas de más de 10.000 elementos.

<FeatureGrid items={[
              {
                icon: Zap,
                label: '3 elementos en el DOM',
                desc: 'Solo se renderizan los slides visibles',
              },
              {
                icon: Zap,
                label: 'Más de 10.000 elementos',
                desc: 'Sin tirones a ninguna escala',
              },
              {
                icon: Zap,
                label: 'Memoria constante',
                desc: 'Los mismos 3 nodos del DOM sea cual sea el recuento',
              },
              {
                icon: Zap,
                label: 'goTo(n)',
                desc: 'Salta a cualquier índice al instante',
              },
            ]} />

<Sandbox
  code={infiniteFullCode}
  title="InfiniteList.tsx"
  height={500}
  stackblitzDeps={['@reelkit/react']}
  stackblitzExtraDeps={{ 'lucide-react': '^0.562.0' }}
>
  <InfiniteListDemo />
</Sandbox>

<Caption>10.000 elementos y solo 3 en el DOM. Usa los botones o escribe un número para saltar.</Caption>

## Demo en vivo: lista que crece [#live-demo-growable-list]

Simula un feed infinito en el que los elementos se cargan bajo demanda, como en TikTok o Instagram. Empieza con 20 elementos; desplázate cerca del final y verás llegar nuevos lotes automáticamente.

<FeatureGrid items={[
              {
                icon: Zap,
                label: 'Recuento dinámico',
                desc: 'Los elementos se cargan al desplazarte',
              },
              {
                icon: Zap,
                label: 'Carga por lotes',
                desc: '20 elementos por lote',
              },
              {
                icon: Layers,
                label: 'Virtualizado',
                desc: 'Sigue habiendo solo 3 en el DOM',
              },
              {
                icon: Radio,
                label: 'Indicador automático',
                desc: 'Los puntos crecen con el contenido',
              },
            ]} />

<Sandbox
  code={growableFullCode}
  title="GrowableList.tsx"
  height={500}
  stackblitzDeps={['@reelkit/react']}
  stackblitzExtraDeps={{ 'lucide-react': '^0.562.0' }}
>
  <GrowableListDemo />
</Sandbox>

<Caption>Desplázate hasta el final: los nuevos elementos se cargan solos. El contador y el indicador crecen a medida que llegan los lotes.</Caption>

## Consejos de rendimiento [#performance-tips]

<KeyPoints>
<KeyPoint title="Memoriza los arrays de datos">
  Envuelve el array de elementos con `useMemo`. Una referencia nueva en cada renderizado provoca una actualización de `count` y un nuevo cálculo de los rangos visibles.
</KeyPoint>
<KeyPoint title="Mantén itemBuilder ligero">
  Se ejecuta con cada cambio del rango visible (normalmente 3 slides). Evita dentro cálculos pesados o efectos secundarios.
</KeyPoint>
<KeyPoint title="Carga datos cerca del final">
  Usa `afterChange` para detectar cuándo el usuario se acerca al final y pide el siguiente lote antes de que se quede sin slides (mira la demo de la lista que crece más arriba).
</KeyPoint>
<KeyPoint title="Desactiva la rueda en páginas con scroll">
  Pon `enableWheel={false}` cuando el slider esté dentro de un diseño con scroll para no capturar el desplazamiento de la página.
</KeyPoint>
</KeyPoints>

<NextSteps
        items={[
          {
            label: 'Referencia de la API',
            path: '/docs/react/api',
            description: 'todas las props disponibles',
          },
          {
            label: 'Reel Player',
            path: '/docs/reel-player',
            description: 'reproductor de vídeo al estilo de TikTok/Reels',
          },
          {
            label: 'Lightbox',
            path: '/docs/lightbox',
            description: 'galería de imágenes y vídeos',
          },
          {
            label: 'Stories Player',
            path: '/docs/stories-player',
            description: 'visor de stories al estilo de Instagram',
          },
        ]}
      />
