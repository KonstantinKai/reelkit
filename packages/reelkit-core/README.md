# @reelkit/core

Framework-agnostic core library with gesture and slider controllers.

## Building

Run `nx build @reelkit/core` to build the library.

## Running unit tests

Run `nx test @reelkit/core` to execute the unit tests via [Vitest](https://vitest.dev/).
