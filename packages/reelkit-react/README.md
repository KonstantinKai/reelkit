# @reelkit/react

React bindings for Reelkit with hooks and components.

## Building

Run `nx build @reelkit/react` to build the library.

## Running unit tests

Run `nx test @reelkit/react` to execute the unit tests via [Vitest](https://vitest.dev/).
