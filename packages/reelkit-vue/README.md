# @reelkit/vue

Vue 3 bindings for Reelkit with composables and components.

## Building

Run `nx build @reelkit/vue` to build the library.

## Running unit tests

Run `nx test @reelkit/vue` to execute the unit tests via [Vitest](https://vitest.dev/).
