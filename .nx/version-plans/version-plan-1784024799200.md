---
'@reelkit/angular': minor
---

New `createOverlayUrlState` — builds a URL-state controller for an overlay, attaching on create and releasing through `DestroyRef`
Core URL-state API is now reachable from the binding: `createUrlStateController`, `createHistoryAdapter`, `indexCodec`, `createIndexLocator`, `indexKey`, and their types
