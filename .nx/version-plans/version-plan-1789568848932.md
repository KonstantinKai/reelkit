---
'@reelkit/react-stories-player': minor
---

Stories player on desktop now fills the window height (with a 16px margin) at 9:16 instead of stopping at 480px wide, so the story is noticeably larger on big screens
