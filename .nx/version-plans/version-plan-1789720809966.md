---
'@reelkit/react': minor
---

Observe now follows a different signal handed to it on a later render, in place of the one it replaced
BREAKING: useViewedState is removed; create the store once with useState(() => createViewedStateController(options)) and attach it in an effect, useEffect(() => store.attach(), [store]), or use createStoriesViewedStateController from @reelkit/stories-core for a stories feed
