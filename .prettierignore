# Add files here to ignore them from prettier formatting
# Prettier ignores package-lock.json by default but not pnpm's lockfile
pnpm-lock.yaml
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular
apps/example-next/next-env.d.ts

# Vendored (minified) self-hosted Plausible analytics script
apps/*/public/js/p.js

.nx/self-healing

# Prettier only knows MDX version 1 and reflows markdown inside JSX blocks — a
# list inside a <Callout> came back as one paragraph. Content files stay hand-formatted.
apps/docs/src/content/**/*.mdx

# Code samples shown on the docs pages, imported as text. Kept byte-for-byte as
# authored so a sample reads the same as the page that used to inline it.
apps/docs/src/content/snippets/
